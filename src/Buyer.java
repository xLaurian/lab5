public interface Buyer {public void buybook(User user);
}

        /*Book book = new book("brasd",1960,"idk_name",18,30);
        if (user instanceof Student) {
            book.setPrice(95/100 * book.getPrice());
        } else if (user instanceof Teacher) {
            book.setPrice(90/100 * book.getPrice());
        } else if (user instanceof Librarian) {
            book.setPrice(80/100 * book.getPrice());
        }
    }
}
